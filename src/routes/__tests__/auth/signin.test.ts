import app from '../../../app';
import request from 'supertest';
import User from '../../../models/user';

describe('POST /signin', () => {
	it('should work', () => {
		expect(true).toBe(true);
	});
});
